From b9d88b3e74d27969a70226c93ba5ee86a69e57ed Mon Sep 17 00:00:00 2001
From: Zac Walker <zac.walker@linaro.org>
Date: Wed, 11 Oct 2023 13:04:45 +0200
Subject: [PATCH 02/18] Add error messages for invalid relocations.

---
 bfd/reloc.c         | 8 ++++++++
 ld/emultempl/pep.em | 3 ---
 2 files changed, 8 insertions(+), 3 deletions(-)

diff --git a/bfd/reloc.c b/bfd/reloc.c
index c9d53bb9e11..9de1d6c617c 100644
--- a/bfd/reloc.c
+++ b/bfd/reloc.c
@@ -1208,6 +1208,14 @@ _bfd_relocate_contents (reloc_howto_type *howto,
   if (howto->negate)
     relocation = -relocation;
 
+  if (howto->bitsize == 21 ||
+      howto->bitsize == 12 ||
+      howto->bitsize == 26)
+    {
+      _bfd_error_handler (_("%pB: %d bit relocation not currently supported.\n"),
+            input_bfd, howto->bitsize);
+    }
+
   /* Get the value we are going to relocate.  */
   x = read_reloc (input_bfd, location, howto);
 
diff --git a/ld/emultempl/pep.em b/ld/emultempl/pep.em
index 2ed4a22be63..85c338fd95f 100644
--- a/ld/emultempl/pep.em
+++ b/ld/emultempl/pep.em
@@ -1183,9 +1183,6 @@ read_addend (arelent *rel, asection *s)
 	    addend = bfd_get_16 (s->owner, buf);
 	}
       break;
-    case 12:
-    case 21:
-    case 26:
     case 32:
       ok = bfd_get_section_contents (s->owner, s, buf, rel->address, 4);
       if (ok)
-- 
2.51.0

